# 2026-04-14

## Codex
- Started migration from the previous OpenTofu spec to the current one described in `Agents.md`.
- Replaced MongoDB-oriented variables and resources with SQL Server-oriented equivalents.
- Kept the Cloudflare tunnel workflow and migrated its inputs to flat variables.
- Switched persistence from host bind mounts to named Docker volumes for Node-RED and SQL Server.
- Added missing templates and scripts required by the Terraform/OpenTofu stack.
- Updated `terraform.tfvars.example` and `terraform.tfvars` to the new variable names.
- Added `config/cloudflare/settings.example.yml` and the local `config/cloudflare/settings.yml`.
- Ran `tofu fmt -recursive`.
- Attempted `tofu validate`, but the local environment failed to instantiate the Cloudflare provider plugin after initialization.
- Updated the Node-RED authentication flow so `terraform.tfvars` now stores the plain password and the bcrypt hash is generated during deploy/runtime, matching `Agents.md`.
- Sanitized local configuration files to remove real SQL Server, MinIO, and Cloudflare secrets from `terraform.tfvars` and `config/cloudflare/settings.yml`.
- Tightened the generated Cloudflare credentials file permission from `0644` to `0600`.
- Removed the auxiliary Cloudflare `settings.yml` dependency so the stack now relies only on `terraform.tfvars`.
