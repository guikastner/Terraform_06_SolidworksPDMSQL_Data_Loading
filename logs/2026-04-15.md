# 2026-04-15

## Codex
- Removed the OpenTofu and Cloudflare implementation from this branch.
- Added a Docker Compose variation with Node-RED, SQL Server, and WebDB.
- Added `.env.example`, `docker-compose.yml`, `compose-up.sh`, and `compose-down.sh`.
- Kept restore and backup scripts for SQL Server and Node-RED in the Compose workflow.
- Removed MinIO references and scripts from the Compose variation.
- Removed backup provisioning concerns from the Compose variation; backup stays exclusive to the OpenTofu branch.
- Added a local `.env` populated with the previously known stack parameters for the Compose variation.
- Removed repository shell scripts from the Compose variation and embedded Node-RED initialization directly in `docker-compose.yml`.
