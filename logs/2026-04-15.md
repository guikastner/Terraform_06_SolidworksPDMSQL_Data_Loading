# 2026-04-15

## Codex
- Added WebDB to the OpenTofu stack with its own container, tfvars inputs, tunnel ingress, and Cloudflare CNAME.
- Kept WebDB internal-only with no host port publishing.
- Prepared the stack for deploy with automatic SQL Server restore from local `.bak` files during `tofu apply`.
